import React from "react";

const Start = ({ click }) => (
  <button
    type="button"
    className="btn btn-outline-light btn-lg"
    onClick={click}
  >
    Start
  </button>
);

export default Start;
